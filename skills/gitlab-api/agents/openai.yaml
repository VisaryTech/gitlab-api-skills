interface:
  display_name: "GitLab API"
  short_description: "Read and post GitLab merge requests, list/fetch pipelines and jobs, and read file data via REST API."
  default_prompt: "Use this skill to fetch merge request details, changes, notes, create merge request notes, list pipelines by ref, fetch pipeline details/jobs, and read repository files from GitLab REST API."
